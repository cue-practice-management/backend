import { RefreshCookieInterceptor } from './refresh-cookie.interceptor';

describe('RefreshCookieInterceptor', () => {
  it('should be defined', () => {
    expect(new RefreshCookieInterceptor()).toBeDefined();
  });
});
